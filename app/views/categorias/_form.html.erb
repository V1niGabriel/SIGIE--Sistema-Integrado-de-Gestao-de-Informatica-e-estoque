<%= form_with(model: categoria, class: "form-content") do |form| %>
  <% if categoria.errors.any? %>
    <div style="background: #fee2e2; color: #991b1b; padding: 15px; border-radius: 8px; margin-bottom: 20px;">
      <strong>Não foi possível salvar:</strong>
      <ul style="margin-top: 10px; margin-bottom: 0;">
        <% categoria.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-grid full">
    <div>
      <%= form.label :nome_categoria, "Nome da Categoria", class: "form-label" %>
      <%= form.text_field :nome_categoria, class: "form-input", placeholder: "Ex: Placas de Vídeo", required: true %>
    </div>
  </div>

  <div class="form-actions">
    <%= link_to "Cancelar", categorias_path, class: "btn-secondary" %>
    <%= form.submit "Salvar Categoria", class: "btn-primary" %>
  </div>
<% end %>