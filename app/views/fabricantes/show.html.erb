<div class="dashboard-container">
  <div class="page-header">
    <h1 class="page-title">Detalhes do Fabricante</h1>
    <div style="display: flex; gap: 10px;">
      <%= link_to "← Voltar", fabricantes_path, class: "btn-secondary" %>
      <%= link_to "Editar", edit_fabricante_path(@fabricante), class: "btn-primary" %>
    </div>
  </div>

  <div class="card detail-card">
    <h2 style="margin: 0 0 20px 0; color: #1f2937;"><%= @fabricante.nome_marca %></h2>
    <div style="text-align: right; border-top: 1px solid #e5e7eb; padding-top: 20px;">
      <%= button_to "Excluir Fabricante", @fabricante, method: :delete, data: { turbo_confirm: "Tem certeza? Isso pode falhar se houver produtos usando esta marca." }, class: "btn-secondary", style: "color: #ef4444; border-color: #fca5a5;" %>
    </div>
  </div>
</div>