<%= form_with(model: fabricante) do |form| %>
  <% if fabricante.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fabricante.errors.count, "error") %> prohibited this fabricante from being saved:</h2>

      <ul>
        <% fabricante.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome_marca, style: "display: block" %>
    <%= form.text_field :nome_marca %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
